---
import { Image } from 'astro:assets';
import type { CollectionEntry } from 'astro:content';
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import FormattedDate from '../components/FormattedDate.astro';
import Header from '../components/Header.astro';
import Interlinks from '../components/Interlinks.astro';

type Props = CollectionEntry<'blog'>['data'];

const { title, description, pubDate, updatedDate, video_date, heroImage, hero_legenda, hero_credit } = Astro.props;

// data da gravação (video_date) distinta da de coleta (pubDate) — GLM-5.2 26/07
const videoDateFmt = video_date
	? video_date.toLocaleDateString('pt-BR', { day: '2-digit', month: 'long', year: 'numeric' })
	: null;
---

<html lang="en">
	<head>
		<BaseHead title={title} description={description} image={heroImage} />
		<style>
			main {
				width: calc(100% - 2em);
				max-width: 100%;
				margin: 0;
			}
			.hero-image {
				width: 100%;
			}
			.hero-image img {
				display: block;
				margin: 0 auto;
				border-radius: 12px;
				box-shadow: var(--box-shadow);
			}
			.image-caption {
				margin: 0.5rem auto 0;
				max-width: 720px;
				color: rgb(var(--gray));
				font-size: 0.85rem;
				line-height: 1.4;
				text-align: center;
			}
			.image-caption .image-caption-text {
				font-weight: 600;
				color: rgb(var(--gray-dark));
			}
			.prose {
				width: 720px;
				max-width: calc(100% - 2em);
				margin: auto;
				padding: 1em;
				color: rgb(var(--gray-dark));
			}
			.title {
				margin-bottom: 1em;
				padding: 1em 0;
				text-align: center;
				line-height: 1;
			}
			.title h1 {
				margin: 0 0 0.5em 0;
			}
			.date {
				margin-bottom: 0.5em;
				color: rgb(var(--gray));
			}
			.video-date {
				margin-bottom: 0.5em;
				color: rgb(var(--gray-dark));
				font-weight: 600;
				font-size: 1.05em;
			}
			.last-updated-on {
				font-style: italic;
			}
		</style>
	</head>

	<body>
		<Header />
		<main>
			<article>
				<div class="hero-image">
					{heroImage && <img width="1020" height="510" src={heroImage} alt={hero_legenda || title} />}
					{heroImage && (hero_legenda || hero_credit) && (
						<p class="image-caption">
							{hero_legenda && <span class="image-caption-text">{hero_legenda}</span>}
							{hero_credit && <span class="image-credit">{hero_legenda ? ' — ' : ''}Foto: {hero_credit}</span>}
						</p>
					)}
				</div>
				<div class="prose">
					<div class="title">
						{videoDateFmt && (
							<div class="video-date">🎙️ Entrevista gravada em {videoDateFmt}</div>
						)}
						<div class="date">
							Publicado em <FormattedDate date={pubDate} />
							{
								updatedDate && (
									<div class="last-updated-on">
										Last updated on <FormattedDate date={updatedDate} />
									</div>
								)
							}
						</div>
						<h1>{title}</h1>
						<hr />
					</div>
					<slot />
				</div>
				<div class="prose" style="padding-top: 0;">
					<Interlinks />
				</div>
			</article>
		</main>
		<Footer />
	</body>
</html>
